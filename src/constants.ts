import { Colour } from "./types";

export const INITIAL_SPEED = 800;
export const SPEED_DECREMENT = 100;
export const MIN_SPEED = 200;

export const AUDIO_PLAYBACK = 2;

export const TIMEOUT_DURATION = 200;
export const LIGHT_START_DELAY = 1000;
export const LIGHT_END_DELAY = 100;
export const ERROR_DELAY = 500;

export const COLOURS: Colour[] = [Colour.GREEN, Colour.RED, Colour.YELLOW, Colour.BLUE];
export const COLOURS_COUNT = COLOURS.length;
